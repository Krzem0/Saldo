<UserControl x:Class="Saldo.Desktop.Wpf.Views.ReferenceListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="8"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ListBox Grid.Row="0"
                 ItemsSource="{Binding Items}"
                 SelectedItem="{Binding SelectedItem}"
                 DisplayMemberPath="Name"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal">
            <Button Content="Add"    Command="{Binding AddCommand}"    Padding="12,4" Margin="0,0,6,0"/>
            <Button Content="Edit"   Command="{Binding EditCommand}"   Padding="12,4" Margin="0,0,6,0"/>
            <Button Content="Delete" Command="{Binding DeleteCommand}" Padding="12,4"/>
        </StackPanel>
    </Grid>
</UserControl>
